# NSFC 引用一致性审核报告

审核日期：2026-08-18  
审核范围：`main.tex` 可达正文与 `references/myexample.bib`

## 摘要

| 项目 | 结果 |
|---|---:|
| 可达 TeX 文件 | 17 |
| 引用实例/唯一 BibKey | 32 / 32 |
| 缺失 BibKey | 0 |
| 重复 BibKey | 0 |
| DOI 格式错误 | 0 |
| P0 | 0 |
| P1 | 0 |

参考文献库共 47 条，正文立项依据引用 32 条。所有引用键均可解析，引用分布覆盖物流物联网、冷链风险、不规则/异步时序、多模态异常、任务导向通信、断续连接与资源约束、漂移和不确定性校准。复检取得21条可比题名，其中20条精确匹配、1条模糊匹配、0条冲突；该结论支持题名层面的身份和领域一致性，不替代摘要/全文语义核查。

在线 DOI 核验因 `nsfc-ref-alignment` 在 Python 3.13 下的正则兼容性错误未能机械执行；本报告使用离线清单、文献调研阶段保存的题录/摘要及前期已核验 DOI 记录完成语义检查。该技能缺陷已按约定本地留痕。

## P0：必须处理

无。

## P1：建议处理

无可由当前材料继续自动修复的 P1。`parametricmachinelearning2024`、`m3dmnrrgb2024`、`conceptdriftadaptation2022` 的 `year` 字段已分别按已核验出版口径改为 2025、2025、2023；BibKey 为保持正文稳定未改名。11条明确会议论文已由 `@article + journal` 规范为 `@inproceedings + booktitle`，未猜测页码。

## 解析器误报说明

确定性脚本把 `tsang2018iotrisk` 与 `tuli2022tranad` 标为缺少 `title`。原始 BibTeX 中两条标题均存在；前者含 `{IoT}`，后者以双层花括号保护 `{TranAD}`，误报来自后备手工解析器对嵌套花括号的处理限制，不是文献字段缺失。

## 引用—论断语义审核

### 物流物联网、运输与港口数字化（`1.1:6`）

原句主张：传感器、边缘计算与智能分析提高过程透明度，但异构数据、动态链路和跨主体协同仍构成约束。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `golpira2021liot` | Logistics IoT review, 2021 | 匹配：物流物联网趋势与研究边界。 |
| `rejeb2020iotlogistics` | IoT in supply chain/logistics bibliometric analysis, 2020 | 匹配：支撑物流与供应链 IoT 研究图景。 |
| `chung2021smartlogistics` | Smart technologies in logistics and transport review, 2021 | 匹配：支撑智能物流技术与应用约束。 |
| `digitaltwinsfor2023` | Digital twins for ports, 2023 | 匹配：支撑港口数字化和跨环节协同。 |

### 冷链监测与风险管理（`1.1:6`）

原句主张：单点传感或事后追溯难以同时满足状态感知和风险管理。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `badiamelis2018coldchain` | Cold chain monitoring review, 2018 | 匹配：支撑冷链监测技术演进。 |
| `tsang2018iotrisk` | IoT-based cold supply-chain risk monitoring, 2018 | 匹配：支撑从监测到风险管理的系统需求。 |

### 不规则/异步时序异常（`1.1:12`）

原句主张：不规则时序、缺失数据、变量异步与异常定位已有对应方法基础。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `che2018grud` | RNNs for multivariate time series with missing values, 2018 | 匹配：缺失值时序建模基础。 |
| `rubanova2019latentode` | Latent ODEs for irregularly sampled time series, 2019 | 匹配：不规则采样连续时间建模。 |
| `practicalapproachto2021` | Asynchronous multivariate time-series anomaly detection/localization, 2021 | 匹配：异步异常检测与定位。 |
| `aaadasynchronousinter2025` | Asynchronous inter-variable anomaly detection, 2025 | 匹配：变量异步关系建模。 |
| `tuli2022tranad` | TranAD multivariate time-series anomaly detection, 2022 | 基本匹配：支撑多变量异常强基线，不单独支撑“异步”主张。 |

### 多模态恢复、工业异常与真实基准（`1.1:12`）

原句主张：缺失模态恢复、工业多模态异常检测和真实场景基准持续发展。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `distributionconsistentmodal2023` | Incomplete multimodal learning, 2023 | 匹配：缺失模态恢复。 |
| `mfganmultimodalfusion2024` | Multimodal fusion for industrial anomaly detection, 2024 | 匹配。 |
| `m3dmnrrgb2024` | RGB-3D noisy-resistant industrial anomaly detection | 语义匹配；年份字段列为 P1。 |
| `robustada2025` | Real-world robustness benchmark for industrial AD, 2025 | 匹配：真实域偏移基准。 |
| `realiadd32025` | Real-world 2D/pseudo-3D/3D IAD dataset, 2025 | 匹配：多模态真实工业数据集。 |

### 任务导向通信（`1.1:14`）

原句主张：语义/任务导向通信以任务损失驱动信息传输。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `xie2021deepsc` | Deep learning enabled semantic communication, 2021 | 匹配：语义通信代表性基础。 |
| `taskorientedsemantic2023` | Task-oriented semantic communication, 2023 | 匹配。 |
| `distributedtaskoriented2024` | Distributed task-oriented communication with multimodal relay/edge intelligence, 2024 | 匹配。 |
| `goalorientedsemantic2024` | Goal-oriented semantic communication for DNN inference, 2024 | 匹配。 |

### 断续连接、采样、分裂推理与资源分配（`1.1:14`）

原句主张：相关研究分别处理间歇连接、能量、带宽与计算约束。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `reviewonrecent2022` | Delay-tolerant networks review, 2022 | 匹配：DTN 研究综述。 |
| `delaytolerantnetworks2025` | DTN connectivity enhancement in remote areas, 2025 | 匹配：远程区域断续连接与时效。 |
| `parametricmachinelearning2024` | Adaptive sampling for efficient IoT collection, 2025 | 匹配：采样与数据收集代价。 |
| `energyawareadaptive2023` | Energy-aware adaptive sampling for constrained IoT, 2023 | 匹配：能量约束采样。 |
| `ispliteeimage2024` | Split computing with early exits, 2024 | 匹配：分裂推理和边缘退出。 |
| `artificialintelligencedefined2024` | Wireless offloading/resource allocation in edge networks, 2024 | 匹配：卸载与资源分配。 |

### 漂移、不确定性与拒判（`1.1:16`）

原句主张：异常检测关注变点、漂移和不确定性；高风险应用需校准并允许拒判。

| BibKey | 题名/年份 | 判断 |
|---|---|---|
| `anomalyandchange2023` | Anomaly/change-point detection under concept drift, 2023 | 匹配。 |
| `conceptdriftadaptation2022` | Concept-drift adaptation for time-series AD, 2023 | 匹配。 |
| `earlyconceptdrift2025` | Early drift detection via prediction uncertainty, 2025 | 匹配。 |
| `uncertaintyawaretime2024` | Uncertainty-aware time-series AD, 2024 | 匹配。 |
| `guo2017calibration` | Calibration of modern neural networks, 2017 | 匹配：概率校准基础。 |
| `geifman2017selective` | Selective classification for deep neural networks, 2017 | 匹配：拒判/覆盖率—风险权衡。 |

## 结论

引用链不存在 P0/P1 结构问题，未发现题名层面的明显张冠李戴或领域冲突。正文使用“提供基础”“持续发展”等审慎表述，没有把单篇文献夸大为统一机制已被解决。仍有11条引文未由当前解析器取得题名或摘要，最终真实性和细粒度语义须在正式提交前通过出版商/DOI元数据人工核验；“引用键对齐”不等于全文语义已经证明。
