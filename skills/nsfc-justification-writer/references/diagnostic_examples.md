# 诊断报告示例（节选）

## 示例 1：结构完整但引用缺失

诊断结果：
- ✅ 结构完整：subsubsection=4
- ❌ 引用缺失：.bib 未找到 keys：Smith2020, Super1957
- ℹ️ 字数统计（中文字符，不含注释）：2210

建议：
- 先补齐/核验 BibTeX 条目（优先提供 DOI/链接或可核验题录信息），再在正文中使用 `\\cite{...}`。
- 由宿主 AI 按 `references/boastful_expression_guidelines.md` 做措辞语义复核；Python 诊断不再对固定短语做自动命中。

## 示例 2：结构缺失（阻塞）

诊断结果：
- ❌ 结构缺失：subsubsection=2，缺少：研究背景、国内外研究现状、现有研究的局限性、研究切入点
- ✅ 引用格式：所有 \\cite{...} 均在 .bib 中存在
- ℹ️ 字数统计（中文字符，不含注释）：830

说明：
- 只有在用户或 legacy 配置显式启用结构检查时，才按其结构提示修复；默认不因缺少 `\\subsubsection` 骨架判定正文为空。
