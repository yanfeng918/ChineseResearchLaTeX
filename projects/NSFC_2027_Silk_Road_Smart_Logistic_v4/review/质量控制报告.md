# NSFC 标书质量控制报告

检查日期：2026-08-18  
基金类型：地区科学基金  
范围：`main.tex` 实际引用的 14 个正文文件、参考文献库、项目事实材料与阶段文档  
执行方式：只读预检 + 3 路 `parallel-vibe` 独立审查 + 主线程聚合

## 执行摘要

项目完整保留货物多模态感知与异常识别、弱网通信/边缘计算/低功耗、仓储多源感知与异常预警三项原始任务，并以“可靠证据状态”建立 C1—C2—C3 接口。第（一）部分 5,292 个中文字符，32 个引用键全部存在，7 个公式块覆盖建模、优化、推断和评价。

当前发现 5 项 P0、14 项 P1 和 6 项 P2。P0 均可基于现有事实和方法文本定点修复，无需虚构新数据；修复前不应编译定稿。最大的事实残余风险是：真实物流数据、运输—仓储配对、2G/3G 实测轨迹、端侧功耗测量和成员既有专长均未落实，正文必须继续限定为可控场景、参数化弱网模拟和条件性验证。

## 定点修复后状态

2026-08-18完成模拟评审聚合和定点修复后，聚焦复审判定可自动修复P0/P1均为0。数学模式、时间目标、C1辨识、C2反事实与泄漏、C3主次关系、响应规则、强基线和年度门控均已闭合；32个引用键无缺失，最终冷构建成功并生成16页PDF。本文后续P0/P1清单保留为修复前审计记录，具体修复映射见 `review/P0P1定点修复报告.md`。

事实残余风险没有被“清零”：直接先导数据、真实弱网/板卡/功耗、跨阶段配对、成员能力、在研项目披露、2026论文状态和A100配额仍须人工补证或核验，正文继续按模拟、估算或条件性分支表述。

## 范围与只读声明

本阶段未修改正文、BibTeX或模板。预检中间产物位于 `.bensz-api/skills/nsfc-qc/v202608182311/artifacts/`；三路独立结果位于 `.bensz-api/task-20260818-2311-nsfc-qc/parallel-vibe/2026-08-18-23-11/`。

## P0：必须修复

### P0-01 行内数学模式损坏

- 位置：`extraTex/1.3.方案及可行性.tex:6,12,20,28,34,40,42,49,63`。
- 问题：`(s_{imj})` 等下标位于文本模式，且 `\tau`、`\bar` 被制表/退格控制字符破坏，`\mathcal`、`\Delta`、`\pi`、`\lambda`、`\operatorname` 的反斜杠丢失。
- 影响：将触发 `Missing $ inserted` 或产生错误渲染。
- 修复：统一改为 `\(...\)` 行内数学，恢复命令并做控制字符扫描和正式编译。

### P0-02 C1检测与C3预测的时间方向冲突

- 位置：`extraTex/1.3.方案及可行性.tex:6-12,42-49`。
- 问题：文字称“未来窗口”，式（1）却定义 `(t-h,t]` 回溯窗口；C3又使用未来风险。
- 修复：保留统一在线信息集，拆分 `\theta^{det}` 回溯检测风险和 `r^{pred}` 前瞻预测风险，明确C2服务于已发生关键异常的截止期告警。

### P0-03 C1状态相关缺失的辨识条件未形式化

- 位置：`extraTex/1.1.立项依据.tex:8,20`；`extraTex/1.3.方案及可行性.tex:14-26`。
- 问题：式（2）没有说明哪些可观测锚点支撑序贯可忽略/正值性，也没有给非可忽略区间的敏感性参数和风险界构造；覆盖损失不能替代辨识论证。
- 修复：明确可审计状态/调度日志的适用域，引入选择敏感性参数 `\Gamma` 与部分辨识区间，并联合评价覆盖率、区间宽度和拒判率。

### P0-04 C2集合条件价值的反事实估计与无泄漏路径不清

- 位置：`extraTex/1.3.方案及可行性.tex:28-40,53-63`。
- 问题：未说明未执行动作的潜在证据、状态转移、行为策略支持及价值学习是否使用未来标签。
- 修复：把风险定义为冻结预测器的单实体条件期望损失；受控试验使用高可靠母流和共同随机数重放；真实日志只在行为策略支持域内离策略估计，采用实体级分割/交叉拟合并冻结最终测试集。

### P0-05 C3核心问题受未落实配对数据制约

- 位置：`extraTex/1.2.内容目标问题.tex:10,22`；`extraTex/1.3.方案及可行性.tex:42-49,57,71`。
- 问题：当前关键问题以运输事件记忆为中心，但申请人确认没有货物级运输—仓储配对数据入口。
- 修复：把仓储漂移下的本地风险校准与拒判设为H3a和独立主问题；运输记忆设为达到实体配对/统计进入条件后的H3b次级检验，H3b失败不影响原始仓储任务完成。

## P1：重要建议

1. 删除未经原始范围支持的“运输累积温湿度暴露”，运输记忆只保留冲击/倾斜—包装损伤和非法开启—货物安全等合理链条。
2. 式（6）当前只衰减到入库时刻，应冻结事件集合并让记忆随仓储时间继续衰减；比较无衰减、共享衰减和事件特异衰减。
3. 为仓储响应增加“校准风险+不确定性→记录/复核/处置建议”的分级规则及漏触发、误触发和触发时延指标。
4. 将协议/资源层零违例硬不变量与识别漏警、误警、超期等风险终点分开，极端断连时转入端侧最低告警或拒判。
5. C3增加后处理校准、漂移再校准和同覆盖率选择性预测强基线，并采用H3a→H3b门控检验。
6. 为三年计划增加数据日志、弱网/资源测量和配对统计功效三类go/no-go门；保留全部模态但共享规范、基线和原型。
7. 在研究内容中显式保留“边缘计算”和“实时监测”原始任务用语。
8. 定义置信下界（LCB）和组平衡时间依赖Brier分数记号，补充式（2）—（7）的关键变量说明。
9. 把新疆—中亚“网络覆盖不连续”改为研究条件设定，除非补充权威地域证据。
10. 引用元数据在线预检：21/32篇取得可比题名，20篇精确、1篇模糊、0篇冲突；无摘要/全文证据的细粒度主张应标为需人工复核。
11. `parametricmachinelearning2024`、`m3dmnrrgb2024`、`conceptdriftadaptation2022` 的正式出版年与当前字段存在口径差异；按Crossref/出版社卷期统一。
12. 会议文献应由 `@article+journal` 规范为 `@inproceedings+booktitle`，但不得猜测页码。
13. `extraTex/2.3.承担项目.tex` 的“无”来自申请人“都不需要”的确认；提交前仍应由科研管理部门复核相关项目披露口径，尤其覆盖主要参与者。
14. 2026年代表性论文的发表/在线/录用状态、作者贡献及A100服务期限和配额须在正式提交前人工核验。

## P2：可选优化

1. 将 `mask`、`bootstrap` 分别统一为“缺失掩码”“按最上层实体自助重采样”。
2. 统一“风险界/风险区间/部分辨识”术语。
3. 打破创新段落三个“相对……本项目……”的平行句式。
4. 删除“避免停留在工程模块拼接”的防御式尾句。
5. 将 `43 pFlops@FP32 / 120 pFlops@FP16` 规范为FP32/FP16精度下的PFLOPS表述，不改变数值。
6. 合并跨章节重复的事实边界声明，保留一次集中说明和各阶段进入条件。

## 引用核查

| 检查项 | 结果 |
|---|---:|
| 唯一引用键 | 32 |
| 缺失/重复键 | 0/0 |
| DOI格式错误 | 0 |
| 取得可比题名 | 21 |
| 精确/模糊/冲突 | 20/1/0 |
| 取得摘要 | 5 |

后备BibTeX解析器把嵌套花括号和单行多字段误判为缺字段；`tsang2018iotrisk`、`tuli2022tranad` 的标题实际存在。HTTP 403只表示自动访问受限，不作为假文献证据。

## 篇幅与结构

- 第（一）部分：5,292个中文字符，低于8,000字上限。
- 14个活动正文：约7,997个中文字符。
- 7个公式块均与模型、优化或评价对应，但变量解释需补齐。
- PDF页数待第14阶段编译复核。

## 最小修改路线

1. 修复数学模式和控制字符。
2. 拆分检测/预测时间目标，补C1辨识与C2无泄漏估计路径。
3. 重构C3为H3a独立主问题+H3b条件性次级检验，并补响应决策。
4. 收紧模拟/实测结论边界、原始任务术语和年度go/no-go门。
5. 修正可核验BibTeX年份/类型，复跑引用与篇幅检查。
6. 完成模拟专家评审后统一实施P0/P1修复，再复检和编译。

## 复现信息

- QC预检：`.bensz-api/skills/nsfc-qc/v202608182311/artifacts/`
- 并行审查：`.bensz-api/task-20260818-2311-nsfc-qc/parallel-vibe/2026-08-18-23-11/`
- 运行模式：智能模式，3路并行，只读正文。
