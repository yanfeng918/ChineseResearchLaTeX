# 编译检查报告

> **状态更新（2026-08-19）：本报告及 `main.pdf` 对应2026-08-18版本。申请人已将2.3在研项目披露改为待核，当前正文尚未重新编译；现有PDF仅为历史工作稿，不得作为提交版本。**

检查日期：2026-08-18  
官方入口：`python scripts/nsfc_build.py build --project-dir .`

## 结果

- 冷构建流程：先由官方 `clean` 清理缓存，再由官方 `build` 完整构建。
- 构建退出码：0。
- 最终产物：`main.pdf`。
- 页数：16页；A4；PDF 1.7。
- 文件大小：373,923字节。
- SHA-256：`12cbf3dc820cf7f701d2c8b4f7a79036a42c3336ca087aa429acedab071d2b0`。

## 错误检查

| 检查项 | 结果 |
|---|---:|
| LaTeX Error / Fatal error | 0 |
| Undefined control sequence | 0 |
| Missing math delimiter | 0 |
| Misplaced alignment | 0 |
| 未定义引用/参考文献 | 0 |
| Overfull box | 0 |

首次构建发现BibTeX会议名称中的双反斜杠导致 `Misplaced alignment tab character &`，并发现一条超宽公式；已分别修复转义和公式换行。最终报告基于修复后的冷构建日志。

## 非阻断 warning

- 12条 `Underfull hbox` 均位于英文参考文献换行，不造成越界或内容缺失。
- `currfile-abspath` 提示未使用 `-recorder`；属于构建参数提示。
- 公共包请求名与提供名提示：`bensz-nsfc-common` / `bensz-nsfc-core`；属于现有公共包兼容层提示。
- `wasy` 10.5pt字体替代和最大0.45pt字号替代；属于现有字体配置提示。
- 未使用的 `caption` figure设置；正文当前无图，不影响输出。

上述 warning 均非本轮正文新增错误，没有出现超宽、缺字、公式截断或引用未解析。

## 页面级验收

- 已将16页逐页渲染到 `tests/final-pdf-check/` 并检查首尾页、研究内容页、公式（1）--（7）所在页、研究基础/工作条件及其他说明页。
- 公式编号、上下标、希腊字母、中文正文和参考文献均可见；未发现裁切、重叠、黑块或控制字符残留。
- `pdftotext -layout` 可正常抽取文本。
