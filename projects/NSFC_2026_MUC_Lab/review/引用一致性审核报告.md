# 引用一致性审核报告

- `extraTex` 中 `\cite{}` 键共 41 个，全部存在于 `references/myexample.bib`。
- 编译后 `main.bbl` 含 41 条，PDF 参考文献已编号渲染。
- 条目均有 DOI、Anthology 或 arXiv 之一；未使用商业语料或虚构文献。
- BibTeX/`gbt7714-nsfc` 对部分会议条目给出缺少 `citedate`/`address` 的既有风格警告，不影响编译。
- `yang2022chineseclip` 为 arXiv 预印本，无会议页码，已在 PDF 中以 URL 形式出现。
- 正文引用均紧贴所支持的方法或数据主张，未在段末无意义堆砌。
